#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Запускаем линтинг только для измененных файлов
npx lint-staged

# Запускаем тесты компонентов UI
npm run test:ui

# Проверяем, что все изменения отформатированы
npm run format:check

echo "✅ Предварительные проверки пройдены успешно!"
